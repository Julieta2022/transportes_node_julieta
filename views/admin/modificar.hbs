<div class="container margenes">
    <div class="row">
        <div class="col">
            <h2>Modificar la novedad</h2>
            <form action="/admin/novedades/modificar" method="post">
            <input type="hidden" value="{{novedad.id}}" name="id">
                <div class="mb-3">
                    <label class="form-label">Título</label>
                    <input type="texto" class="form-control" name="titulo" value="{{novedad.titulo}}">
                </div>
                <div class="mb-3">
                    <label class="form-label">Subtítulo</label>
                  <textarea class="form-control" rows="3" name="subtitulo">{{novedad.subtitulo}}</textarea>
                </div>
                 <div class="mb-3">
                    <label class="form-label">Cuerpo</label>
                  <textarea class="form-control" rows="3" name="cuerpo">{{novedad.cuerpo}}</textarea>
                </div>
                <button type="submit" class="btn btn-primary">Modificar</button>
            </form
            {{#if error}}
            <p>{{message}}</p>
            {{/if}}
        </div>
    </div>
</div>
 